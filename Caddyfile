http:// {
    # Serve static files first
    handle_path /static/* {
        root * /app
        file_server
    }

    # Serve media files
    handle_path /media/* {
        root * /app
        file_server
    }

    # Proxy all other requests to the Django app
    handle {
        reverse_proxy server:8000
    }
}
